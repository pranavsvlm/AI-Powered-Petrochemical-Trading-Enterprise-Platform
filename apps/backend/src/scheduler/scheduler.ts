// Placeholder scheduler entrypoint — no scheduled tasks registered yet.
function main(): void {
  // eslint-disable-next-line no-console
  console.log('scheduler: placeholder entrypoint, no tasks scheduled');
}

main();
