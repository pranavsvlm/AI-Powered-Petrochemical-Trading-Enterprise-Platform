{
  "name": "@platform/mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo \"React Native app not yet scaffolded\" && exit 0",
    "build": "echo \"React Native app not yet scaffolded\" && exit 0",
    "typecheck": "echo \"no source yet\" && exit 0",
    "lint": "echo \"no source yet\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@platform/core": "workspace:*"
  }
}
