# @platform/ai

AI Router, Prompt Engine, and Agent SDK stubs powering the platform's AI Gateway.

> Status: skeleton — subfolders (`router`, `prompt-engine`, `agent-sdk`) exist with placeholder exports only.
