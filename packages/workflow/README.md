# @platform/workflow

Automation/workflow engine stub. Depends on `@platform/event-bus` for its transport layer —
workflow steps react to and emit domain events published on the event bus.

> Status: skeleton — no implementation yet.
