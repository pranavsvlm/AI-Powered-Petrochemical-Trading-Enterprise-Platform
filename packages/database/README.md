# @platform/database

Prisma schema and repository interfaces for centralized data access. The Prisma schema lives
here (not in `apps/backend`) so every app/package shares one source of truth.

> Status: skeleton — `prisma/schema.prisma` has no models yet.
