{
  "name": "@modules/company",
  "version": "0.1.0",
  "private": true,
  "description": "Company/tenant profile and organizational settings module.",
  "main": "api/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint application infrastructure api --max-warnings=0",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "types": "api/index.ts",
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@platform/core": "workspace:*",
    "@platform/types": "workspace:*",
    "@platform/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
