{
  "name": "@modules/knowledge",
  "version": "0.1.0",
  "private": true,
  "description": "Internal knowledge base and document module.",
  "main": "api/index.ts",
  "scripts": {
    "build": "echo \"no build target yet\" && exit 0",
    "typecheck": "echo \"no source yet\" && exit 0",
    "lint": "echo \"no source yet\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@platform/core": "workspace:*",
    "@platform/types": "workspace:*"
  }
}
