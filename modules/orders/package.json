{
  "name": "@modules/orders",
  "version": "0.1.0",
  "private": true,
  "description": "Sales order management module (doc 13): confirmation, fulfillment, amendments.",
  "main": "api/index.ts",
  "types": "api/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint domain application infrastructure api --max-warnings=0",
    "test": "jest"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@platform/core": "workspace:*",
    "@platform/types": "workspace:*",
    "@platform/database": "workspace:*",
    "@platform/event-bus": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^20.16.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
